jdk:
  - openjdk16
before_install:
  - ./ensure-java-16.bat install
install:
  - if ! ./ensure-java-16.bat use; then source ~/.sdkman/bin/sdkman-init.sh; fi
  - java -version
  - bash gradlew.bat publishToMavenLocal